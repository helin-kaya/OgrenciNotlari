@using OgrenciNotlariEL.ViewModels;
@model StudentLessonScoreDTO
@{
    ViewData["Title"] = "AddStudentLessonScore";
}

<h1>AddStudentLessonScore</h1>
<div class="row">
    
    
    <div class="col-8 offset-2 offset-2">

        <form asp-controller="Home" asp-action="AddStudentLessonScore" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            @*<input type="hidden" value="@ViewBag.StudentId" id="studentId" />*@
            <input type="hidden" asp-for="StudentId" />
            @*<div class="form-group">
                <label>Öğrenci Id </label>
                <input type="text" class="form-control" asp-for="StudentId" />

            </div>*@
            @*<div class="form-group">
                <label>Öğrenci Id </label>
                <input type="text" class="form-control" asp-for="StudentId" />

            </div>*@



            @*<div class="form-group">
                <label>Ders Adı</label>
                <select id="lessonselect" class="form-control">
                    <option value="0">****DERS SEÇİNİZ****</option>
                    @foreach (LessonDTO item in ViewBag.AllLesson)
                    {
                        <option value="@item.Id">@item.Name</option>

                    }
                </select>

            </div>*@
            <div class="form-group">
                <label>Ders Id </label>
                <input type="text" class="form-control" asp-for="LessonId" />

            </div>
            <div class="form-group">
                <label>Not </label>
                <input type="text" class="form-control" asp-for="Score" />

            </div>



            <div class="form-group">
                <button type="submit" class="btn btn-primary">KAYDET</button>
            </div>
        </form>
    </div>

</div>